{plugins, [
    {pc, "1.15.0"}
]}.

{project_plugins, [
    {erlfmt, "1.5.0"},
    {rebar3_ex_doc, "0.2.31"},
    {rebar3_hex, "7.0.8"}
]}.

{erlfmt, [write]}.
{provider_hooks, [{pre, [{compile, {pc, compile}}, {clean, {pc, clean}}]}]}.
% NIF still built by pc during transition; main.c-derived sources match c_src/main.c
% (worker.c is compiled separately via the Makefile).
{port_specs, [{"priv/erlfdb_nif.so", ["c_src/main.c", "c_src/atoms.c", "c_src/resources.c", "c_src/util.c"]}]}.
% port_env compiler / linker flags dynamically generated in rebar.config.script
% pre_hooks/post_hooks for c_src/Makefile (worker binary) are also injected by
% rebar.config.script so that env vars (FDB API version, lib dir, etc.) flow
% through to make.

{profiles, []}.
{eunit_opts, [debug_info, verbose]}.
{cover_enabled, true}.
{cover_export_enabled, true}.
{hex, [{doc, #{provider => ex_doc}}]}.
{ex_doc, [
    {source_url, <<"https://github.com/foundationdb-beam/erlfdb">>},
    {extras, [
        <<"README.md">>,
        <<"LICENSE">>,
        <<"CHANGELOG.md">>,
        <<"notes/configuration.md">>,
        <<"notes/thread-design.md">>,
        <<"notebooks/kv_queue.livemd">>,
        <<"notebooks/tutorial-elixir.livemd">>
    ]},
    {main, <<"readme">>}
]}.
{edoc_opts, [{preprocess, true}, {macros, [{erlfdb_compile_time_api_version, 730}]}]}.

{shell, [
    {apps, [erlfdb]},
    {config, "config/shell.config"}
]}.
